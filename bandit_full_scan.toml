[project]
name = "gradio-rag-prototype"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "gradio>=6.13.0",
    "openai>=2.33.0",
]

[tool.bandit]
exclude_dirs = ["tests"]
skips = ['B101']
